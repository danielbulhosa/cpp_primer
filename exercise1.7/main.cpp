#include <iostream>

/*
 * comment pairs /* */ cannot nest.
   "cannot nest" is considered source code,
   as is the rest of the program. This is
   because the first */ terminates the first
   /*
 */

///*
// * Best way to comment out many lines of code
// * (that could have multilines comments) is to
// * prefix each line with single line comment //
// * characters, as shown here.
// */
//int main() {
//    return 0;
//}

int main() {
    return 0;
}
